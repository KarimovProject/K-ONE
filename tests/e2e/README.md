# End-to-end tests

Playwright scenarios start in Phase 1 when user-visible workflows exist. The Python
Playwright dependency and this isolated test surface are ready; no browser binaries are
downloaded automatically during Phase 0 setup.

